@model RecipeCube.Areas.Admin.ViewModels.RecipeViewModel

@* 如果有圖片顯示該圖片，沒有則顯示預設圖片 *@
<img id="Photo"
     src="~/img/recipe/@(string.IsNullOrEmpty(Model.Photo) ? "icon_large.png" : Model.Photo)"
     alt="@Model.Photo"
     style="width:256px;height:256px" class="mb-3" />
